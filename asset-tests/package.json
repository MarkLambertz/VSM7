{
  "name": "vsm7-asset-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Isolated Playwright (real-browser) tests for the standalone VSM7 front-end assets (e2e-robustness-check.html, vsm.html). Self-contained: does NOT touch the dependency-free main repo package.json or Codex's tests/.",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
